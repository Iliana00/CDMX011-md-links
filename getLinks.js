const fs = require('fs');
const path = require('path');
const http = require('http');

function searchinglinks (files) {
 
    console.log('OMG Estos son un archivo' + files)          
 
}

exports.searchinglinks = searchinglinks;